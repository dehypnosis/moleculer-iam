{
  "name": "moleculer-iam-workspace",
  "private": true,
  "workspaces": {
    "packages": [
      "pkg/*",
      "examples"
    ]
  },
  "scripts": {
    "build-all": "yarn workspace moleculer-iam build && yarn workspace moleculer-iam-app-renderer build-server && yarn workspace moleculer-iam-app-renderer build",
    "publish-all": "yarn workspace moleculer-iam publish && yarn workspace moleculer-iam-app-renderer publish"
  }
}
