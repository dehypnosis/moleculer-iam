{
  "name": "moleculer-iam-workspace",
  "private": true,
  "workspaces": {
    "packages": [
      "pkg/*",
      "examples"
    ]
  },
  "scripts": {
    "build-all": "yarn workspace moleculer-iam build && yarn workspace moleculer-iam-app build",
    "publish-all": "yarn workspace moleculer-iam publish && yarn workspace moleculer-iam-app publish",
    "test": "yarn workspace moleculer-iam test && yarn workspace moleculer-iam-app test",
    "test-with-examples": "yarn workspace moleculer-iam test-with-examples && yarn workspace moleculer-iam-app test",
    "coverall": "yarn workspace moleculer-iam coverall"
  }
}
